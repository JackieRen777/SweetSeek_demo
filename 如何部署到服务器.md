# 如何部署代码到服务器

## ⚠️ 重要概念

**Kiro提交到GitHub ≠ 服务器自动更新**

完整流程需要3步：
1. ✅ Kiro提交到GitHub（代码上传）
2. ⚠️ 服务器拉取代码（需要手动）
3. ⚠️ 重启服务器应用（需要手动）

---

## 🎯 推荐方法：使用Kiro + 终端命令

### 第1步：本地测试（必须！）

在Kiro终端运行：
```bash
bash test_local.sh
```

或手动运行：
```bash
python3 app.py
```

访问 http://localhost:5001 测试功能

---

### 第2步：Kiro提交到GitHub

在Kiro界面操作：

1. **打开源代码管理**（左侧栏第三个图标）

2. **暂存更改**
   - 点击 "更改" 旁边的 "+" 号
   - 或单独点击每个文件的 "+"

3. **输入提交信息**
   ```
   修正结构逻辑错误
   ```

4. **提交**
   - 点击 "✓" 提交按钮

5. **同步到GitHub**
   - 点击 "同步更改" 或 "推送"
   - 等待推送完成

---

### 第3步：部署到服务器

#### 方法A：使用一键部署脚本（推荐）

在Kiro终端运行：
```bash
bash deploy_to_server.sh
```

脚本会自动：
- ✅ 推送到GitHub
- ✅ SSH到服务器
- ✅ 拉取最新代码
- ✅ 重启应用
- ✅ 健康检查

#### 方法B：手动执行命令

在Kiro终端运行：
```bash
ssh root@8.137.32.247
```

输入密码后，在服务器上执行：
```bash
cd /www/wwwroot/FCN_SweetSeek
git pull origin RenJiaqi
pkill -f "python3 app.py"
sleep 3
nohup python3 app.py > logs/app.log 2>&1 &
ps aux | grep "python3 app.py" | grep -v grep
```

---

### 第4步：验证线上效果

1. 访问 http://sweetseek.top
2. 强制刷新：`Cmd + Shift + R`（Mac）
3. 测试功能是否正常

---

## 📊 完整流程图

```
┌─────────────────────────────────────────┐
│ 1. 本地开发和测试                        │
│    bash test_local.sh                   │
│    访问 http://localhost:5001           │
└─────────────────┬───────────────────────┘
                  │ 测试通过 ✅
                  ↓
┌─────────────────────────────────────────┐
│ 2. Kiro提交到GitHub                     │
│    - 暂存更改 (+)                       │
│    - 提交 (✓)                           │
│    - 同步更改                           │
└─────────────────┬───────────────────────┘
                  │ 推送成功 ✅
                  ↓
┌─────────────────────────────────────────┐
│ 3. 服务器拉取代码（手动！）              │
│    ssh root@8.137.32.247                │
│    cd /www/wwwroot/FCN_SweetSeek        │
│    git pull origin RenJiaqi             │
└─────────────────┬───────────────────────┘
                  │ 拉取成功 ✅
                  ↓
┌─────────────────────────────────────────┐
│ 4. 重启服务器应用（手动！）              │
│    pkill -f "python3 app.py"            │
│    nohup python3 app.py > logs/app.log &│
└─────────────────┬───────────────────────┘
                  │ 重启成功 ✅
                  ↓
┌─────────────────────────────────────────┐
│ 5. 验证线上效果                          │
│    http://sweetseek.top                 │
│    Cmd+Shift+R 强制刷新                 │
└─────────────────────────────────────────┘
```

---

## 🚀 快速命令参考

### 本地测试
```bash
bash test_local.sh
# 或
python3 app.py
```

### 一键部署
```bash
bash deploy_to_server.sh
```

### 手动部署
```bash
# 在本地Kiro终端
ssh root@8.137.32.247

# 在服务器上
cd /www/wwwroot/FCN_SweetSeek && \
git pull origin RenJiaqi && \
pkill -f "python3 app.py" && \
sleep 3 && \
nohup python3 app.py > logs/app.log 2>&1 &
```

---

## ❓ 常见问题

### Q1: 我在Kiro提交了，为什么网站没更新？
**A**: Kiro提交只是上传到GitHub，服务器需要手动拉取代码。

### Q2: 我拉取了代码，为什么网站还是旧的？
**A**: 需要重启服务器应用才能加载新代码。

### Q3: 我重启了应用，浏览器还是显示旧版本？
**A**: 需要强制刷新浏览器：`Cmd+Shift+R`（Mac）或 `Ctrl+Shift+R`（Windows）

### Q4: 如何确认服务器代码已更新？
**A**: 在服务器上运行：
```bash
cd /www/wwwroot/FCN_SweetSeek
git log -1
```
查看最新的commit是否是你刚才提交的。

### Q5: 如何确认应用已重启？
**A**: 在服务器上运行：
```bash
ps aux | grep "python3 app.py" | grep -v grep
```
应该能看到进程信息。

---

## 🎓 最佳实践

### ✅ 推荐做法

1. **本地测试优先**
   - 永远先在 localhost:5001 测试
   - 确认功能正常再提交

2. **小步提交**
   - 每个功能测试通过就提交
   - 便于出问题时回滚

3. **使用脚本**
   - 使用 `deploy_to_server.sh` 减少人为错误
   - 自动化重复操作

4. **查看日志**
   - 部署后查看服务器日志
   - 确认没有错误

### ❌ 避免做法

1. ❌ 不测试就提交
2. ❌ 提交后不部署到服务器
3. ❌ 部署后不验证
4. ❌ 直接在服务器上修改代码
5. ❌ 忘记重启应用

---

## 📞 遇到问题？

### 查看服务器日志
```bash
ssh root@8.137.32.247
tail -f /www/wwwroot/FCN_SweetSeek/logs/sweetseek.log
```

### 健康检查
```bash
ssh root@8.137.32.247
curl http://localhost:5001/api/health
```

### 回滚代码
```bash
ssh root@8.137.32.247
cd /www/wwwroot/FCN_SweetSeek
git log --oneline -5  # 查看最近5次提交
git reset --hard <commit-id>  # 回滚到指定版本
pkill -f "python3 app.py"
nohup python3 app.py > logs/app.log 2>&1 &
```

---

## 🎯 记住这个流程

```
本地测试 ✅
    ↓
Kiro提交 ✅
    ↓
服务器拉取 ⚠️ (需要手动)
    ↓
重启应用 ⚠️ (需要手动)
    ↓
线上验证 ✅
```

**关键：Kiro提交后，必须手动在服务器执行 git pull 和重启应用！**
