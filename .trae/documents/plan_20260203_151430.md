没问题，我们采用**最稳妥、最透明**的手动触发模式。

我将从头引导您完成整个部署流程。整个过程由 `deploy.sh` 脚本自动化处理，**您只需要在终端提示输入密码时，输入一次密码即可**。

### 部署流程计划

1.  **提交本地代码**：我会先确保您本地的所有最新修改（包括刚才配置的 `.env`）都已经提交到 Git 仓库。
2.  **推送到 GitHub**：将代码推送到远程 `RenJiaqi` 分支，作为中转。
3.  **执行部署脚本**：运行 `deploy.sh`。
    - **您的操作**：脚本运行后，终端会提示 `root@8.137.32.247's password:`，此时请输入密码 `Fcn509509` 并回车。
    - **脚本自动执行**：
        1.  SSH 连接服务器。
        2.  从 GitHub 拉取最新代码。
        3.  **关键步骤**：自动将服务器上的 `.env` 文件中的 API Key 更新为您刚才提供的 Key（因为 Git 不会同步 `.env`）。
        4.  重启服务器上的 Python 应用。
        5.  进行健康检查，确保网站上线。

这个流程既保证了安全性（密码由您输入），又保证了部署的完整性（代码+配置同步更新）。

准备好后，请确认执行，我将为您启动部署。