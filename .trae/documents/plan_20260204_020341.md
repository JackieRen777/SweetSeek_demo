### 请手动执行修复命令

由于我们需要输入密码，请您直接复制以下两条命令到下方的终端中执行：

#### 第一步：强制开启离线模式
这条命令会将离线环境变量写入服务器配置文件：
```bash
ssh root@8.137.32.247 "echo 'HF_HUB_OFFLINE=1' >> /www/wwwroot/FCN_SweetSeek/.env && echo 'TRANSFORMERS_OFFLINE=1' >> /www/wwwroot/FCN_SweetSeek/.env && echo '✅ 离线模式已配置'"
```
*(粘贴后回车，输入密码 `Fcn509509`)*

#### 第二步：重启服务
配置完成后，重启服务以生效：
```bash
ssh root@8.137.32.247 "pkill -f 'python.*app.py'; nohup /www/wwwroot/FCN_SweetSeek/venv/bin/python3 /www/wwwroot/FCN_SweetSeek/app.py > /www/wwwroot/FCN_SweetSeek/logs/app.log 2>&1 & echo '✅ 服务已重启，正在后台加载模型...'"
```
*(同样输入密码)*

---
执行完这两步后，请等待约 **10-15 秒**（让模型加载完成），然后尝试访问 http://sweetseek.top 看看是否恢复正常！