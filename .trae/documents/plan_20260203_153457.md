既然 `deploy.sh` 的交互式输入在当前环境下始终无法显示，我们直接采用最原始、最可靠的 **“分步手动执行”** 方案。

这虽然稍微麻烦一点（需要复制粘贴命令），但**绝对不会卡住**，因为每一条命令都是在您自己的控制下执行的。

### 手动部署流程
您只需要在终端中**依次复制并运行**以下 3 条命令：

#### 1. 连接服务器
手动 SSH 登录，这一步一定会提示输入密码。
```bash
ssh root@8.137.32.247
```
*(输入密码 `Fcn509509` 并回车登录)*

#### 2. 在服务器上拉取代码并更新配置
登录成功后，**一次性复制粘贴**以下所有命令块到服务器终端执行：
```bash
cd /www/wwwroot/FCN_SweetSeek && \
git pull origin RenJiaqi && \
sed -i 's/DEEPSEEK_API_KEY=.*/DEEPSEEK_API_KEY=sk-vhasvsfskdkwhzafgyftrvyjikxodvwfngzyazexwsybucdc/' .env && \
echo "代码和配置更新完毕"
```

#### 3. 重启服务
继续在服务器终端执行：
```bash
pkill -f "python.*app.py"; \
nohup /www/wwwroot/FCN_SweetSeek/venv/bin/python3 app.py > logs/app.log 2>&1 & \
echo "服务正在重启，请稍候..."
```

---
**我将为您停止当前卡住的脚本，并准备好终端环境供您手动操作。请确认。**