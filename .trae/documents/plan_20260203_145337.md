我已完成对项目文件的分析，为您制定了清理冗余文件和修复本地运行的详细计划。

### 第一阶段：清理冗余脚本
项目中存在大量重复功能的脚本和临时说明文件（尤其是中文命名的）。我们将保留最完善的版本并清理其他文件。

**1. 待删除的冗余文件（共 8 个）：**
- `分步执行命令.txt`、`手动部署步骤.txt`、`服务器手动操作指南.txt`、`服务器完全重置.txt`（说明文档应整合到 README 或删除）
- `完全重新部署.sh`、`服务器完整修复流程.sh`、`重置命令.sh`（功能与主部署脚本重复且具有破坏性）
- `update_server.sh`（功能已被主部署脚本覆盖）

**2. 待保留并优化的核心脚本：**
- **`deploy.sh`**：作为**唯一的主部署脚本**。
    - *优化计划*：目前它包含 7 个步骤，会建立多次 SSH 连接（导致需要输多次密码且易断连）。我将把所有远程操作（拉取代码、环境检查、重启服务、健康检查）合并为**一次 SSH 连接**，彻底解决连接重置问题。
- **`push.sh`**：重命名为 `git_push.sh`，用于便捷提交代码。

### 第二阶段：修复本地运行 (localhost:5001)
目前无法访问是因为缺少本地配置和依赖环境。

**1. 配置本地环境：**
- 创建本地 `.env` 配置文件（基于 `.env.production` 复制，开启调试模式）。
- 确保 Flask 应用监听正确端口。

**2. 安装依赖与启动：**
- 检查并安装 `requirements.txt` 中的依赖（如 `llama-index`, `flask` 等）。
- 尝试启动 `app.py` 并排查可能的初始化错误（如向量数据库路径或模型加载问题）。

执行此计划后，您的项目目录将变得整洁，且能在本地通过 `http://localhost:5001` 正常访问。请确认执行。