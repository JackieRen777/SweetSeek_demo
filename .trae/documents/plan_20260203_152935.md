非常抱歉给您带来困扰！您遇到的问题是：虽然脚本看起来在“连接中”，但因为终端配置问题，**密码输入提示被隐藏了**，或者根本没有弹出来，导致您无法输入密码。

为了解决这个问题，我制定了以下修复计划：

### 核心问题
当前的 SSH 命令没有正确分配伪终端（Pseudo-terminal），导致在自动化脚本中密码提示符无法正常显示，看起来像是“卡住”了。

### 修复方案
我将修改 `deploy.sh`，强制启用 SSH 的交互模式。

1.  **修改 SSH 参数**：将 `ssh -t` 改为 `ssh -tt`（强制分配伪终端），这能确保无论在什么环境下，密码输入提示都能正确显示。
2.  **简化脚本**：移除可能导致干扰的复杂 Heredoc 结构，改用更直接的命令执行方式，确保您能清楚看到每一步。

### 执行步骤
1.  **修改脚本**：我会立即为您更新 `deploy.sh`。
2.  **重新运行**：更新后，您再次运行 `./deploy.sh`，这次您一定会看到明确的 `password:` 提示。

请确认执行此修复。